<form method="post" action="{{ path('operacion_delete', {'id': operacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operacion.id) }}">
    <button class="btn">Delete</button>
</form>
